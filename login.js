const username = document.getElementById('tilin69')
const password = document.getElementById('tilin')
const button = document.getElementById('button')

button.addEventListener('click', (e) => {
        e.preventDefault()
        const data = {
            username: tilin69.value,
            password: tilin.value
        }

        console.log(data)
    })